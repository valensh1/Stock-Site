import React from 'react';

const BackButton = () => {
	return (
		<div>
			<button
				type="button"
				className="btn btn-primary btn-lg about-page-button"
			>
				BACK
			</button>
		</div>
	);
};

export default BackButton;
